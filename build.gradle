group 'io.wedeploy.example'
version '1.0-SNAPSHOT'

task run(type: Exec) {
  executable './lein'
  args 'run'
  environment PORT: 8080
  environment NREPL_PORT:7001
  environment LEIN_HOME: '.'
}

task wrapper(type: Wrapper) {
  gradleVersion = '2.12'
}